# Environment Variables Example
# Copy to .env.local if you need to override anything locally.

# Build mode. Set to "1" for the GitHub Pages static export (out/); unset
# builds standalone for a local server preview. CI sets this itself, so you
# only need it when reproducing the Pages build by hand.
GH_PAGES=1
